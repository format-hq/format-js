# Format project

Build PDFs with web tech. Each document lives in its own folder under `documents/`.

## Get started

Install dependencies, then start Format Studio:

```sh
npm install
npm run dev
```

Studio opens in your browser with a live preview. Edit `documents/welcome` and the preview updates as you save.

## Learn more

Read the docs at https://format.dev/docs
